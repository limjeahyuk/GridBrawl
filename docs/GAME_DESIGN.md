# GridBrawl — 게임 디자인 문서 (GDD)

> 이 문서는 **GridBrawl**의 게임 룰·설계 의도·현재 구현 상태를 한곳에 모은 단일 진실 공급원(single source of truth)입니다.
> Claude(및 사람 개발자)는 전투/카드/토너먼트 관련 작업 전에 이 문서를 먼저 읽고, 변경이 생기면 이 문서를 같이 갱신합니다.
>
> 구성: ① 한 줄 요약 → ② 참고작(이누야샤 데몬 토너먼트) 룰 분석 → ③ 우리 게임의 설계 명세(의도) → ④ 현재 구현 상태(코드 기준) → ⑤ **명세 ↔ 구현 차이 & 미결정 사항** → ⑥ 용어집.
>
> ⚠️ **가장 중요한 섹션은 ⑤번입니다.** "설계 명세"와 "현재 코드"가 여러 지점에서 다릅니다. 작업 시작 전 반드시 ⑤를 확인하세요.

---

## ① 한 줄 요약

*이누야샤 데몬 토너먼트*처럼 **1:1 토너먼트 기반 카드 전투**를 핵심으로, 캐릭터 선택·성장과 짧은 플레이 세션을 결합한 그리드 액션 대전 게임.

- 장르: 턴제 / 카드 선택 / 전술 격투
- 한 턴에 **카드 3장**을 골라 순서대로 실행, 두 파이터가 동시에 진행
- HP가 0이 되면 승패 결정 → 다음 상대로 진행하는 토너먼트(건틀릿) 구조

---

## ② 참고작 분석 — Inuyasha: Demon Tournament

> 우리 게임의 "게임 방식"은 이 플래시 게임과 동일하게 가는 것이 목표입니다. 아래는 웹 리서치로 확인한 원작 룰입니다(출처는 문서 맨 아래).

### 개요
- 애니메이션 *이누야샤* 캐릭터로 싸우는 **턴제 1:1 전술 격투**. (단순화된 Tactics Arena Online + Street Fighter 느낌)
- 플레이어와 CPU가 **매 라운드 카드 3장을 선택**하고, 그 카드들이 **하나씩 차례로 공개되며** 행동이 펼쳐진다(양측 동시 진행).

### 전장(그리드)
- 2D 격자 전장에서 두 파이터가 마주 본다(스크린샷 기준 가로로 긴, 대략 3행 격자).
- 각 캐릭터는 격자 위 한 칸을 차지하고, **이동 카드**로 격자를 이동한다.

### 카드 구성
모든 카드에는 다음 정보가 표시된다: **이름 / 데미지(DM) / 에너지 비용(EN) / 3×3 사거리 그리드**.
- **3×3 그리드 표기**: 정중앙 칸 = 캐릭터 자신의 위치. 강조(빨강)된 칸 = 그 행동이 영향을 주는 범위.
- **이동 카드 (Movement)**: 카드 하단 화살표 방향으로 이동.
- **공격 카드 (Attack)**: 에너지를 소모, 상대가 사거리 칸 안에 있으면 데미지를 입힌다.
- **가드 카드 (Guard)**: 그 턴에 받는 피해를 **최대 15까지** 막는다.
- **에너지 회복 카드 (Energy)**: 에너지 바를 **15** 회복.

### 자원
- **에너지(EN)**: 공격/이동 등에 소모. 카드마다 비용이 다름. (라운드 사이 일부 회복)
- **HP**: 공격이 적중하면 카드 데미지만큼 차감. 0이 되면 패배.

### 진행/보상
- 적을 쓰러뜨리면 **사혼의 구슬 조각** 획득 + **미스터리 카드 5장 중 1장 선택**하여 덱에 추가(특수 공격 / 이동 강화 / 회복 등).
- 토너먼트(사다리) 형태로 점점 강한 상대와 대결.

### 캐릭터(원작)
이누야샤, 카고메, 셋쇼마루, 미로쿠, 산고, 키쿄, 나라쿠 등. 캐릭터마다 고유 기술/특수공격 보유.

> 📌 우리 게임은 원작 캐릭터를 **그대로 쓰지 않고** 오리지널 캐릭터로 대체했습니다. 룰·시스템만 차용합니다(저작권 회피 + 오리지널리티). 테마는 **다크 판타지**(VESPER/MAUL/DIRGE/SABLE/CAIRN/PYRE) — 2026-07-31 리스킨 전에는 사이버 아레나(VOLT/TITAN/NOVA/CIPHER/AEGIS/EMBER)였고, 대응표는 ④ "로스터 리스킨"에 있습니다.

---

## ③ 설계 명세 (의도) — 사용자가 정의한 룰

> 이것은 **목표로 하는 디자인**입니다. 현재 코드와 다른 부분이 있으며, 차이는 ⑤에 정리.

### 게임 흐름
1. 캐릭터 선택
2. 각 캐릭터는 **공용 카드 + 고유(직업) 카드**를 가진다.
3. 공용 카드: 이동 카드, 가드 카드, 에너지 회복 카드
4. 고유 카드: 공격 카드 (에너지를 써서 데미지. 카드마다 **범위 / 에너지 / 데미지**가 다름)
5. **매 턴 카드 3장 선택**
6. 상대와 함께 3장을 **순서대로** 실행
   - **카드 실행 순서: ① 이동 카드 → ② 수비(가드) 카드 → ③ 공격 카드**
7. HP가 다 닳으면 승리 또는 패배

### 시작 배치 (2D 격자)
```
+----+----+----+----+
| P1 |    |    | P2 |   ← 두 캐릭터가 격자 양끝에서 마주 보고 시작
|    |    |    |    |
+----+----+----+----+
```
(구현 확정: **3행 × 6열**. 두 파이터는 가운뎃줄 양 끝에서 마주 보고 시작.)

### 카드 명세 (2026-07-03 갱신 — 공용/고유 이원화)

> 사용자 방침: **이동 카드는 모두 동일(공용)**. 공용에는 *별로 안 좋은* 공격·방어 카드도 포함되어 누구나 쓸 수 있는 바닥 라인을 이루고, **진짜 강한 카드는 각 캐릭터(직업)의 고유 카드**다. 고유 카드에는 다양한 특수 능력(넓은 범위, 강한 공격력, 때리면서 기력 흡수 등)이 붙는다. 추후 **뽑기/구매로 카드를 획득해 덱을 조합**하는 시스템으로 확장 예정(미구현).

#### 1) 공용 카드 (Common) — 의도적으로 약함
| 카드 | 에너지 | 효과 | 쿨타임 |
|---|---|---|---|
| 이동 `>` `<` `^` `v` (1칸) | 0 | 해당 방향 1칸 이동 | 0턴 |
| 이동 `>>` `<<` (2칸) | 0 | 좌/우 2칸 이동 | 1턴 |
| 스트라이크 | 10 | 앞뒤 1칸 **10 데미지** (약공) | 0턴 |
| 펄스 샷 | 10 | 앞뒤 2칸째 1칸씩 **10 데미지** (견제) | 0턴 |
| 가드 | 10 | **50 데미지 방어** | 1턴 |
| 브레이스 | 10 | **30 데미지 방어** (약가드) | 0턴 |
| 에너지 회복 | 0 | **에너지 50 회복** | 0턴 |

#### 2) 고유(직업) 카드 — 캐릭터마다 4장, 특수 능력 보유
캐릭터별 전용 카드. 대부분 공격이지만 종류 제한은 없다(예: AEGIS의 전용 가드 "아이언 커튼"). 공격 카드 특수 능력 목록(엔진 지원, 조합 가능):

| 능력 | 필드 | 효과 |
|---|---|---|
| 기력 흡수 | `drain: N` | 적중 시(가드로 막혀도) 상대 기력을 최대 N 빼앗아 흡수 |
| 흡혈 | `leech: N` | 피해를 실제로 입히면 체력 N 회복 |
| 관통 | `pierce` | 상대 보호막을 무시(소모시키지도 않음)하고 피해 |
| 넉백 | `push: N` | 적중 시 상대를 공격자 방향 기준 뒤로 N칸 밀어냄(벽·공격자 셀에서 멈춤) |
| 방벽 전개 | `selfShield: N` | 기력 지불 성공 시 자신 보호막 +N (빗나가도 적용) |
| 반동 | `recoil: N` | 기력 지불 성공 시 자신 체력 -N (빗나가도 적용) |

> 핵심: **선택한 3장을 정해진 순서(이동→수비→공격)대로 실행하여 그에 맞는 행동을 하는 것**이 가장 중요한 메커니즘.

---

## ④ 현재 구현 상태 (코드 기준)

> ⚠️ 아래는 **실제로 코드에 들어가 있는 동작**입니다. ③의 의도와 다른 부분이 있으니 ⑤와 함께 보세요.

### 기술 스택 / 실행
- **React 19 + TypeScript + Vite** (SPA, 단일 캔버스형 무대).
- 개발: `npm run dev` · 빌드: `npm run build` · 타입검사: `npm run typecheck`
- 외부 게임 엔진 없음. 전투는 순수 TS 클래스(`CardBattle`)로 시뮬레이션하고 React로 렌더.

### 화면 흐름 (`src/App.tsx`)
`title → select(캐릭 선택) → bracket(대진표) → fight(전투) → result(결과)` 상태 머신.
- 토너먼트는 "건틀릿": 나머지 모든 파이터를 한 번씩 상대, 난이도 `easy,easy,normal,normal,hard`로 상승, 마지막이 최강. (`src/game/tournament.ts`)

### 전투 모델 (`src/battle/`) — **2D 격자** (방향 A로 구현 완료)
- **전장은 2D 격자**: `GRID_COLS = 6` × `GRID_ROWS = 3`. 셀 `{ col, row }`. col은 오른쪽으로, row는 아래로 증가.
- 시작 위치 `START_CELLS = [{0,1}, {5,1}]` — 가운뎃줄 양 끝에서 마주 봄. p0(플레이어)는 +col(오른쪽), p1(상대)는 −col(왼쪽)을 바라본다(`facing`).
- 자세(Stance) 개념은 **제거**됨 — 세로축은 이제 진짜 격자 이동(위/아래).
- 한 턴 = 카드 3장. **고른 슬롯 순서(1→2→3)대로** 해소하되, **한 슬롯 안에서** 나·상대 카드는 **타입 우선순위(이동 → 수비 → 공격)** 로 처리한다. 즉 "이동→수비→공격"은 전역 묶음이 아니라 *같은 슬롯 내 둘 중 누가 먼저 처리되는지* 정하는 규칙.
- **전투 생성 옵션(`BattleOpts`)** — 로그라이크 전용 훅. 미지정이면 예전 그대로라 **PvP·봇전·튜토리얼은 불변**이다: `chars`(스탯을 갈아끼운 CharacterDef — 몬스터), `passives`(유물 merge 결과), **`startHp`(2026-07-30, 층 사이 HP 이월 — 지정한 쪽만 그 체력으로 시작, 1..maxHp 클램프)**. 상세는 [ROGUELIKE.md](ROGUELIKE.md).

#### 카드 종류 (`CardKind`) — `src/battle/types.ts`
`move` | `attack` | `guard` | `energy` | `heal`(2026-07-23). 모든 카드에 `cooldown`(사용 후 잠기는 턴 수, 0/없음=재사용 가능).
- **이동 방향(`MoveDir`)**: 직교 4방향 + **대각선 4방향**(`up-right/up-left/down-right/down-left`, 2026-07-23). `MOVE_DELTA`가 델타를, `MIRROR_DIR`가 멀티 좌우 반전 짝을 정의(엔진은 정규 좌표, `BattleScreen.faceCard`가 side1 표시 라벨만 미러).
- **힐(`heal`)**: 기력을 체력으로 바꾸는 지원 카드(`c-repair`: `healCost:20`→`healHp:20`, 쿨1). 엔진 `resolvePrep`에서 처리(기력 부족 시 `nofuel`), 회복량은 `Step.heal`로 실어 초록 `+N` 연출 재사용. `planAffordable`이 `healCost`를 계산 → UI가 못 내는 힐 카드를 선택 불가 처리.

#### 공용 카드 (`COMMON_CARDS`) — `src/battle/cards.ts` (2026-07-03 확장)
| id | 이름 | kind | 효과 | 에너지 | 쿨타임 |
|---|---|---|---|---|---|
| `m-right` / `m-left` / `m-up` / `m-down` | 오른쪽/왼쪽/위/아래 | move | 해당 방향 1칸 (`>`/`<`/`^`/`v`) | 0 | **0** |
| `m-right2` / `m-left2` | 오른쪽/왼쪽 대시 | move | 좌·우 2칸 (`>>`/`<<`) | 0 | **1** |
| `c-strike` | 스트라이크 | attack | 앞뒤 1칸 **10 데미지** — 약한 공용 공격 | 10 | **0** |
| `c-shot` | 펄스 샷 | attack | 앞뒤 **2칸째만** **10 데미지** — 약한 견제 사격 | 10 | **0** |
| `c-guard` | 가드 | guard | 기력 **10**(`GUARD_COST`) 소모, 이번 턴 피해 최대 **50**(`GUARD_BLOCK`) 흡수 | 10 | **1** |
| `c-brace` | 브레이스 | guard | 기력 10 소모, 이번 턴 피해 최대 **30** 흡수 — 약가드 | 10 | **0** |
| `c-energy` | 원기 회복 | energy | 기력 **35**(`ENERGY_GAIN`) 회복 | 0 | **1** (2026-07-15 하향: 50→35, 쿨0→1) |
- ⚠ **약공 3장(`c-strike`·`c-shot`·`c-jab`)은 2026-08-04부터 플레이어의 기본 공격이 아니다** — 직업별 기본기(`CharacterDef.basics`, 아래 "직업 기본기" 절)가 그 자리를 대신한다. 여기 남아 있는 이유는 **몬스터 덱(`monsters.ts`)·보스 스크립트·검사 스크립트가 계속 쓰기 때문**이고, `c-shot`은 덱 빌더 선택 풀에도 남아 있다(빼면 그걸 고른 저장 덱이 조용히 줄어든다).
- 추가 페이싱 장치: **턴 시작 시 패시브 기력 회복 `ENERGY_REGEN = 20`** (양측, 최대치 cap. 2026-07-15 하향: 30→20 — "매턴 3공격" 억제). 명세에는 없던 디자인 보강 — 원기 카드가 쿨일 때 교착 방지.

#### 고유 카드 (캐릭터별 4장, `src/data/roster.ts` — 필드명 `cards`)
공격 필드: `range`(타격 셀 오프셋 배열), `damage`, `energyCost`, `cooldown:0` + 특수 능력 필드(아래).
- **`range`의 오프셋 `{ df, du }`**: `df`=앞쪽 칸 수(+는 상대 방향), `du`=위쪽 행 수(+는 위). 엔진이 `facing`으로 `df`의 좌우를 뒤집어 실제 셀로 변환(`targetsOf`).
- 적중 조건: 상대 셀이 변환된 타격 셀 집합에 포함될 때.
- 범위 헬퍼: `fwd(n)`(앞 n칸), `beam(a,b)`(같은 줄 직선 빔), `both(n)`(앞뒤 n칸째), `beamBoth(a,b)`(앞뒤 대칭 직선), `bar(df)`(앞/뒤 df칸의 위·중·아래 세로줄, 음수 가능), `CROSS`(상하좌우 십자), `FORK`(앞뒤 대각선 네 갈래 X자 — 정면 사각).
- **양방향 규약(2026-07-14)**: 교차(대시로 뛰어넘기)가 의도된 룰이므로 **대부분의 공격은 앞뒤를 함께 커버**하고, 그 대신 한쪽 사거리를 줄인다(예: 앞 3칸 빔 → 앞뒤 2칸씩). 캐릭터마다 1~2장은 **전방 전용**(램 프레스·이온 랜스·노바 블래스트·널 사이펀·실드 배시·오버히트 블리츠)으로 남겨 방향 싸움이 유지된다. 카드 미니 범위표(`RangeChart`)는 -3..3 창으로 확장.
- **특수 능력**(`types.ts`의 `drain/leech/pierce/push/selfShield/recoil`, 엔진 `computeAttack`·`applyOutcome`이 적용, ③의 표 참고):
  - `drain`·`push`는 **적중 시**(가드로 막혀도) 발동. `leech`는 **피해가 실제로 들어갔을 때만**.
  - `selfShield`·`recoil`은 **기력 지불 성공 시** 발동(빗나가도 적용, 기력 부족 `nofuel`이면 미발동).
  - `pierce`는 보호막을 소모시키지 않고 통과(EMBER의 `shieldBreak` 패시브와 별개).
  - 넉백 방향은 공격자 `facing` 방향(벽/공격자 셀에서 멈춤). 동시 트레이드에선 두 공격 판정 후 넉백을 순서대로(호스트 먼저) 적용.
  - UI: 카드 하단에 능력 칩(`CardFace`의 `abilityTags` — 관통/기력흡수/흡혈/넉백/방벽/반동), 전투 중 반동은 자기 몸의 `-N`, 흡수는 대사창 `⚡+N`으로 표시.
- 캐릭터별 직업 콘셉트(각 고유 4장): **VOLT** 기력 흡수 스피드스터(포크 라이트닝 `FORK` X자·서지에 `drain`), **TITAN** 광역+넉백 브루저(램 프레스 `push:2`, 슬램 `push:1`), **NOVA** 초장거리 포격수(이온 랜스 `pierce`, 패시브 기력 +20), **CIPHER** 흡혈 암살자(사이펀 `leech+drain`, 페이즈 `leech`, 패시브 `lifesteal`), **AEGIS** 방패 하이브리드(잽·드라이브 `selfShield`, **전용 가드 "아이언 커튼"** 70방어/20기력/쿨2, 패시브 방벽 +20), **EMBER** 하이리스크 러셔(블리츠·인페르노 `recoil`, 패시브 `revive:50` — 잿불 부활).
- **수치 규약(2026-07-14)**: 게임에 노출되는 모든 수치(데미지·기력 비용·방어량·회복·HP·시작 기력)는 **10의 배수**로 통일 — 가독성 우선. HP(2026-07-25 밀착 타격 재조정 — 아래 밸런스 패스): CIPHER 145, EMBER 156, VOLT 157, NOVA 162, AEGIS 164, TITAN 172. 단, 세부 수치(비용 12 등)는 5의 배수 허용으로 완화. ⚠️ **2026-07-25부터 밸런스 수치는 이 규약보다 우선한다** — 시뮬 검증 결과 10/5 배수로 반올림하면 캐릭터 승률이 무너진다(밸런스 패스 참고).

#### 3직업 개편 — 6종 → 3종 (2026-08-01)

로스터를 **6명에서 3직업으로 줄였다**. 6명은 서로 "조금씩 다른" 상태라 고를 이유가 약했다. 3직업은 **각자 못 하는 것을 확실히 정해** 선택이 성격을 가르게 한다.

| id | 이름 · 칭호 | 체력 | 패시브 | 잘하는 것 | **못 하는 것** |
|---|---|---|---|---|---|
| `warrior` | CAIRN · Oathbound Warden | 205 | 불침의 서약: 피해감소 6 · 매턴 체력 +4 | 내구·회복·기절·넉백·반사 | 느린 기동, **원거리 없음** |
| `archer` | SABLE · Ashen Fletcher | 162 | 독니: 공격 +6 · 적중 시 독 3 | 기동·한방·관통·독 | 낮은 체력, **밀착 사각** |
| `mage` | DIRGE · Hollow Oracle | 155 | 혼백의 등불: 매턴 기력 +12 · 보호막 +7 | 넓은 범위·화상·빙결·보호막 | 낮은 단일 화력, **비싼 기력** |

- ⚠ **id를 새로 팠다**(`warrior`/`archer`/`mage`). 아래 리스킨 절의 "id는 절대 바꾸지 않는다"는 **이번 개편으로 폐기됐다** — 6종 체계 자체가 사라져 대응시킬 대상이 없다. 옛 id를 가리키는 **저장 덱은 마이그레이션하지 않고 기본 덱으로 떨어뜨린다**(사용자 결정). 코드에는 옛 id가 하나도 남아 있지 않다.
- **직업 카드 수가 균일하지 않다** — 전사 5장(전용 가드 `war-wall` 포함), 궁수·마법사 4장. `decks.ts`의 `PRESET_DECKS`가 고유 카드를 먼저 넣고 남는 자리를 `PRESET_FILLERS`로 채워 7장을 맞춘다.
- **빌드는 유물이 완성한다**(`game/relics.ts`). 직업은 출발점만 정하고 런 중 유물이 방향을 가르므로, 같은 직업도 런마다 다른 빌드가 된다. 직업별 시그니처는 `SIGNATURE`의 `warrior`/`archer`/`mage`.
- **지속 상태이상이 직업 정체성이 됐다** — 궁수=독(`arc-venom` 6·`arc-rain` 8), 마법사=화상·빙결(`mag-spark` 4·`mag-frost` 빙결1·`mag-flame` 6·`mag-doom` 8+빙결1). 엔진에만 있던 기능(2026-08-01 1단계)이 드디어 카드에 연결됐다. **그래서 상태이상은 더 이상 밸런스 중립이 아니다.**

**밸런스 재조정 완료(2026-08-01).** 개편 직후엔 WARRIOR 31.0 / MAGE 1.3 / ARCHER 0.7%(밴드 30.3%p)로 궁수·마법사가 클리어 불가였다. `SIGNATURE.runEffect`만 조정해 **밴드 2.1%p**로 잡았다 — 5시드 × 900런 평균 WARRIOR 26.9 / MAGE 26.5 / ARCHER 26.1(최악 시드 3.2%p). 숙련도 기울기 easy 13.4% → normal 20.6% → hard 24.0%.

| 직업 | 런 실효 수치(`SIGNATURE.runEffect`) |
|---|---|
| `warrior` | 피해감소 8 · 매턴 체력 +3 · 반사 6 |
| `archer` | 최대체력 +48 · 피해감소 7 · 흡혈 5 · 공격 +9 · 적중 시 독 4 · 지속피해 위력 +25% |
| `mage` | 최대체력 +38 · 피해감소 6 · 매턴 기력 +10 · 보호막 +16 · 적중 시 화상 3 |

- **PvP 불변** — `runEffect`는 런 전용이고 `scripts/simulate.ts`는 `relics.ts`를 import하지 않는다. ⚠ `npm run sim`은 시드가 없어 실행마다 ±0.05턴 흔들리므로 회귀 판정에 쓰지 말 것.
- ⚠ **단일 시드 튜닝은 과적합된다** — 같은 수치로 시드만 바꿔도 밴드가 1.5~7.3%p로 널뛴다. 여러 시드 평균으로 판정할 것.
- **궁수는 수치가 아니라 구조 문제였다** — 카드 4장 중 3장이 밀착 사각인데 몬스터가 이동하게 되면서 계속 붙는다. ⚠ 화력·독을 올리면 **오히려 떨어진다**(독 위력 +45% 실험: 24.7%→22.2%). 버티는 힘을 줘야 한다.
- ⚠ **훅 민감도** — 궁수 `lifesteal` 4→5 = +5%p(지배 변수), `maxHpBonus`는 48 근처 포화(55→62에서 +0.1%p). 마법사 `turnEnergy` 10 ≈ `maxHpBonus` 14.

#### 직업 기본기 — 공용 약공을 직업별로 가름 (2026-08-04)

3직업 개편 뒤에도 **기본 공격은 셋이 똑같았다**. 로그라이크는 공용 약공 3장(`c-strike`/`c-shot`/`c-jab`)으로 출발하고 직업 카드는 보상으로 버는 구조라, **직업을 골라도 1~5층이 같은 싸움**이었다("각 캐릭터 기본 공격이 다 똑같은 게 아쉽다"). 그래서 `CharacterDef`에 **`basics` 3장**을 새로 뒀다(공용 약공은 그대로 남아 몬스터·검사 스크립트가 계속 쓴다).

**총합 화력은 공용과 거의 같게 맞췄다** — 1층이 여전히 쉬워야 하므로. 차이는 **어디를 때리느냐**에 있다.

| 직업 | 기본기 | 사거리 | 피해 | 기력 |
|---|---|---|---|---|
| `warrior` | 거친 도끼질 `war-hew` | 앞뒤 1칸 | 14 | 10 |
| | 발치 후리기 `war-ring` | **주변 8칸**(`RING` = `CROSS`+`FORK`) | 10 | 12 |
| | 어깨 밀치기 `war-shove` | 앞뒤 1칸 · 넉백 1 | 9 | 8 |
| `archer` | 짧은 화살 `arc-nock` | 앞뒤 1~2칸 | 10 | 10 |
| | 먼 겨냥 `arc-mark` | 앞뒤 2~3칸 · **밀착 사각** | 12 | 12 |
| | 단검 긋기 `arc-knife` | 앞뒤 1칸 | 11 | 8 |
| `mage` | 불티 `mag-ember` | 십자 4칸(`CROSS`) | 11 | 12 |
| | 서리 조각 `mag-shard` | X자 4칸(`FORK`) | 9 | 10 |
| | 망령의 손길 `mag-touch` | 앞뒤 세로 3줄 6칸(`barBoth(1)`) | 10 | 14 |

- **모양이 곧 정체성이다** — 전사는 전부 밀착 1칸(원거리 없음)이라 한 대가 제일 아프고, 궁수는 가로로 제일 길되 제일 약하며, 마법사는 제일 넓되 기력이 제일 비싸다(매 턴 기력 +12 패시브가 그 비용을 감당하는 자리).
- ⚠ **궁수 기본기의 밀착 사각은 저격 한 장(`arc-mark`)에만 뒀다.** 기본기 셋이 다 사각이면 붙인 상대에게 아무것도 못 해 1층에서 막힌다 — 2026-08-01에 확인한 궁수 구조 문제와 같은 함정이다. 약점(밀착 사각)은 **직업 고유 카드** 쪽에 그대로 남아 있다.
- **배선** — `basics[0]`은 덱 고정 카드(`decks.ts`의 `fixedCardsFor`, 예전엔 공용 `c-strike`), 나머지 2장은 덱 빌더 선택 풀(`poolFor`). 런은 세 장 모두 시작 덱에 들어간다(`run.ts`의 `startingDeck`). ⚠ **`deckFor`에 반드시 포함돼야 한다** — 멀티 락스텝의 상대 플랜 복원(`net/session.ts`)과 AI 기본 풀이 이 목록으로 카드 id를 되찾으므로, 빠지면 카드가 조용히 사라진다. `monsterChar`는 `basics: []`로 덮어써 아트를 빌려온 직업의 기본기가 몬스터에게 딸려 가지 않게 한다.
- **밸런스(5시드 × 900런 `--sweep`, 시드 12345/777/2024/555/99)** — 도입 전 WARRIOR 29.3 / ARCHER 25.7 / MAGE 26.7 · 밴드 **3.6%p** → 도입 후 WARRIOR 31.3 / ARCHER 30.5 / MAGE 29.7 · 밴드 **1.5%p**. 클리어율이 ~3%p 오르고 밴드는 좁아졌다(앞뒤 대칭 사거리라 명중률이 오른 몫이 크고, 밀착 사각이 줄어든 궁수가 가장 많이 올랐다: +4.8%p). 1~2층 통과율은 100%.
- ⚠ **PvP(`npm run sim`)는 같이 움직였다** — AI 기본 풀이 `deckFor`가 되면서 기본기가 **추가**되기 때문이다(런처럼 공용 약공을 대체하는 게 아니다). ARCHER 85→70 / WARRIOR 49→69 / MAGE 17→11: 전사·궁수는 거의 대등해졌고 마법사는 더 나빠졌다. **PvP는 3직업 개편 이후 한 번도 맞추지 않은 상태**(④의 남은 과제)라 이번에도 손대지 않았다.

> 아래 밸런스 기록·수치표는 전부 **6종 시절 측정치**라 이 개편 이후로는 무효다.

#### 로스터 리스킨 — 사이버 아레나 → 다크 판타지 (2026-07-31, 6종 시절)

> 📌 아래 표는 **6종 시절의 기록**이다. 위 3직업 개편으로 `volt`/`titan`/`cipher`/`ember`는 사라졌고, 살아남은 세 이름(CAIRN·SABLE·DIRGE)도 **id와 역할이 바뀌었다**(SABLE은 Bloodletter → Ashen Fletcher로 콘셉트까지 교체). 옛 시뮬 로그를 읽을 때의 대조표로만 쓸 것.
아트를 픽셀 다크 판타지 스프라이트(기사·광전사·원령·도적·악마)로 갈아끼우면서 로스터의 **표현만** 그 세계관에 맞췄다. **바뀐 것은 이름·칭호·소개문·패시브 이름·강조색뿐이고, id·카드·수치·훅은 전부 그대로다** — 밸런스 재측정이 필요 없다(`npm run sim`/`sim:run` 결과 불변).

| 코드 id | 옛 이름 | **새 이름** | 칭호 | 옛 패시브명 → **새 패시브명** | 강조색(accent / accent2) |
|---|---|---|---|---|---|
| `volt` | VOLT | **VESPER** | Stormbound Knight | 오버차지 → **뇌운의 가호** | `#8fb6d6` / `#d9b463` (창백한 뇌광·낡은 금) |
| `titan` | TITAN | **MAUL** | Ruin Berserker | 장갑판 → **무쇠 비늘** | `#c9713a` / `#d9a45e` (녹슨 철·놋쇠) |
| `nova` | NOVA | **DIRGE** | Hollow Oracle | 플라스마 코어 → **혼백의 등불** | `#a578cf` / `#6fc0b0` (원령 보라·시체빛 청록) |
| `cipher` | CIPHER | **SABLE** | Bloodletter | 데이터 흡수 → **피의 갈증** | `#5aa06d` / `#3f9a90` (독기 녹·청동 녹청) |
| `aegis` | AEGIS | **CAIRN** | Oathbound Warden | 상시 방벽 → **불침의 서약** | `#6d8ac4` / `#a7b8d4` (차가운 강철·은빛) |
| `ember` | EMBER | **PYRE** | Ashen Devil | 잿불 부활(유지) | `#cf5347` / `#e0913f` (잿불 진홍·불씨 주황) |

- ~~**id는 절대 바꾸지 않는다.**~~ → **폐기(2026-08-01).** 3직업 개편에서 id를 새로 팠다. 대신 그 대가로 **저장된 계정 덱이 전부 무효**가 됐다(RTDB `decks/<uid>`의 `charId`가 옛 id를 가리키면 기본 덱으로 폴백). 시그니처 유물 id·몬스터 `baseArtId`·`SHEETS`가 id를 키로 쓴다는 사실 자체는 그대로이므로, **다음에 id를 또 바꾸면 같은 대가를 다시 치른다.**
- 색은 **원래의 6색 색상환(청록·주황·보라·초록·파랑·빨강)을 유지한 채 채도만 낮춘 것**이다 — 보드 위에서 두 파이터를 구별하는 역할이 그대로 살아 있고, `--accent`가 어두운 배경 위 글자색으로도 쓰이므로 명도는 읽히는 선에서 멈췄다.
- 시그니처 유물(`game/relics.ts`의 `SIGNATURE`)도 같은 이름 체계로 맞췄다: 뇌운의 인장 / 무쇠 비늘 / 혼백의 등불 / 피의 성배 / 불침의 서약 / 불사조 깃털. **`runEffect` 수치는 불변.**
- ⚠ **아래 밸런스 기록과 수치표는 옛 코드네임(VOLT·TITAN…)을 그대로 둔다** — 시뮬 로그·과거 측정치와 대조해야 해서 고치면 오히려 추적이 끊긴다. 이 표를 대응표로 쓸 것.
- **스프라이트 이관 완료**(2026-08-01): 3직업 전원이 픽셀 스프라이트다 — `warrior`→hero-knight, `archer`→bandit-light, `mage`→bandit-heavy. 로스터가 6→3으로 줄면서 필요한 시트도 6→3이 돼 한 번에 끝났다. `SHEETS`에 없는 id는 자동으로 SVG 폴백. 원본 팩·굽는 절차·시트별 측정값은 [`public/sprites/README.md`](../public/sprites/README.md). 몬스터는 `baseArtId`로 직업 시트를 빌려 쓰므로 **사실상 전부 픽셀**이 됐다.
  - ⚠ **마법사에 중장 도적 시트를 임시로 붙였다** — 지팡이 팩이 없어서다. 활·지팡이 팩을 구하면 `SHEETS`만 갈아 끼우면 된다. 팩마다 프레임 구성이 달라(Bandits는 대기 4·달리기 8·공격 8) `banditClips()`로 `STD_CLIPS`를 통째로 교체한다.
  - **전장 배경도 같이 갈았다(2026-08-01)**: 리스킨은 `--neon-*` **값만** 바꾸는 방식인데, 격자 셀·HUD 발광이 옛 시안(`#2ff3ff`)을 **리터럴로 박아** 둬서 팔레트를 갈아도 판만 사이버로 남아 있었다. 리터럴을 전부 `color-mix(… var(--neon-*) …)`로 돌리고, 셀 면을 그을린 돌 색으로, 바닥은 **아래 횃불(주황) + 위 달빛(강철빛)**으로 바꿨다. 등속으로 지나가던 스캔광(`floor-sweep`)은 그 자체가 SF 신호라 걷어내고 밝기만 흔드는 횃불 일렁임(`torch-flicker`)으로 교체. ⚠ 앞으로도 전장 색은 리터럴로 박지 말 것.
  - ⚠ 폴백 SVG와 스프라이트는 **바닥선이 다르다** — SVG는 `.fighter`의 `translate(-50%, -72%)`로 셀 **앞쪽 모서리**에 서고, 스프라이트는 `--footpx`로 셀 **중앙**에 선다(1280×720에서 약 40px 차이). 같은 줄에 섞여 서면 한쪽이 떠 보인다. 여섯 명이 다 스프라이트가 되면 사라지지만, 몬스터는 SVG로 남으니 로그라이크에선 계속 보인다.
- **아직 남은 사이버 잔재**: 고유 카드 이름(스파크 잽·아크 볼트·플라스마…)과 일반 유물 이름(재생 나노봇·예비 배터리…), 타이틀 화면 문구(`THE GRID · DEMON GAUNTLET`). 카드 id를 안 건드리면 이름 교체는 순수 표시 변경이라 안전하다(다음 후보).

#### 캐릭터 패시브 (`Passive`, `src/data/roster.ts`)
각 캐릭터는 고유 패시브를 1개 가진다. 엔진이 정해진 시점에 자동 적용(`resolveTurn` 턴 시작 / `computeAttack` 공격 판정). 캐릭터 선택 화면 `detail__passive`에 설명 노출.

| 캐릭터 | 패시브 | 필드 | 적용 시점 |
|---|---|---|---|
| VOLT | 매 턴 기력 +10, 보호막 +10 | `turnEnergy:10, turnShield:10` | 턴 시작(전역 회복·실드 리셋 직후) |
| TITAN | 받는 공격 피해 **-9**(0 미만 불가. 2026-07-15 20→10, 2026-07-25 10→9 — **-10이면 20데미지 카드가 절반으로 잘려 과강**) | `damageReduction:9` | 피격 데미지 계산(실드 흡수 후) |
| NOVA | 매 턴 기력 +20 (플라스마 코어) | `turnEnergy:20` | 턴 시작 |
| CIPHER | 공격으로 피해를 주면 체력 **+15**(고정치, 2026-07-15 상향) | `lifesteal:15` | 공격 적중(데미지 확정 후) |
| AEGIS | 매 턴 보호막 +15 (2026-07-15 하향) | `turnShield:15` | 턴 시작 |
| EMBER | **잿불 부활** — 쓰러져도 전투당 한 번 HP 50으로 부활 (2026-07-14, 기존 가드 브레이크 대체) | `revive:50` | KO 직후(슬롯 해소·독안개 각각의 KO 판정 시점) |

- 적용 순서(공격 판정 한 건): `shieldBreak`로 상대 실드 0 → 실드 흡수 → `damageReduction` 차감 → 데미지 확정 → `lifesteal` 회복(공격자 HP, 최대치 cap). `shieldBreak`는 엔진 지원은 유지되나 현재 소유 캐릭터 없음.
- **부활(`revive:N`)**: KO 판정 직후 발동 — 아직 안 썼다면 HP N으로 되살아나고(`state.revived`로 1회 제한), `phase:'revive'` 스텝이 삽입돼 전투 화면에 초록 `+N`과 "잿불 부활" 대사가 표시된다. 양쪽 동시 KO 시 둘 다 부활 시도 후 다시 판정. 랜덤 없음 → 멀티 락스텝 안전.
- 패시브 보호막/기력은 가드·원기 카드 위에 **추가로** 쌓인다(같은 `s.shield`/`s.energy` 풀). 턴 시작 실드 리셋(`s.shield=[0,0]`) 직후 부여되므로 매 턴 갱신.
- `planAffordable`·UI 기력 예산은 플레이어 `turnEnergy`를 더해 계산(VOLT/NOVA가 더 비싼 플랜 가능).
- 회복은 `Step.heal`로 전달돼 전투 화면에서 초록 `+N`으로 표시(`.fighter__heal`).

#### 턴 해소 순서 (`CardBattle.resolveTurn`) — `src/battle/engine.ts`
턴 시작: 가드 실드 0으로 초기화 → 양측 패시브 기력 +20(`ENERGY_REGEN`). 이어서 **슬롯 0→1→2 순서**로, 각 슬롯에서 그 슬롯의 두 카드(나·상대)를 **우선순위 `이동(0) < 수비(1) < 공격(2)`** 로 정렬해 낮은 쪽부터 처리:
- **이동(move)** — 해당 방향 이동. 벽에 막히면 멈춤. **상대 셀은 통과·정지 모두 가능 — 겹침 허용**(2026-07-14 변경, 기존 "멈출 수 없음" 규칙 폐지). 넉백(push)도 벽에서만 멈추고 공격자 셀 위로 밀 수 있다. 겹친 상태에서도 **대부분의 카드는 서로 때릴 수 있다**(아래 "밀착 타격"). 엔진 `applyMove`·AI 시뮬레이션·UI 미리보기(`applyMovePreview`) 모두 동일 규칙. 화면에선 겹친 두 파이터를 좌우로 **9px씩** 비켜 표시(`.fighter--stacked-*`) — 예전 26px은 셀이 좁은 폰에서 "한 칸 옆"으로 읽혀 2026-07-25에 축소.
- **수비(defense)** — `guard`(기력 10 지불, 실드 +50) / `energy`(기력 +35).
- **공격(attack)** — 정렬된 board(앞선 이동/수비 반영) 기준으로 판정. 사거리 밖=`whiff`, 기력 부족=`nofuel`, 적중 시 상대 실드부터 차감(전부 흡수되면 `blocked`).
- **밀착 타격(point-blank, 2026-07-25)** — 어떤 카드의 `range`도 자기 셀 `{df:0,du:0}`을 덮지 않으므로, **겹친 상대는 사거리와 별개로 카드의 `pointBlank` 필드로 판정**한다. 기본값은 "맞는다"(`pointBlank !== false`) — **공격 카드 24장(공용 3 + 직업 기본기 9 + 직업 고유 12) 중 19장 = 79%가 밀착 타격 가능**(2026-08-04 직업 기본기 도입 후 재집계). 예외 5장: `c-shot`(펄스 샷·두 칸째 전용) + 궁수의 `arc-shot`·`arc-venom`·`arc-rain`·`arc-mark`(기본기 저격). **밀착 사각이 궁수에 몰려 있는 것이 3직업 약점 설계의 핵심이다** — 붙으면 궁수가 죽는다. 이 4장은 카드 UI에 **`밀착사각` 능력칩**이 붙는다(`CardFace.abilityTags`). 엔진 `computeAttack`·AI `hits()`·UI 범위 표시(`attackCells`의 `foe` 인자)가 같은 규칙을 공유하며, AI는 밀착 타격 수단이 있으면 겹침을 피하지 않고 오히려 올라탄다(`canPointBlank`).
- **한 슬롯에 양측 다 공격**이면(둘 다 우선순위 2) **동시 계산 후 동시 적용**(상호 트레이드·동시 KO 가능 — 승자는 타이브레이크로, 아래). 그 외엔 낮은 우선순위 카드가 먼저 → 다음 카드는 갱신된 board를 본다(예: 내가 먼저 이동하면 상대 공격을 회피, 상대가 먼저 가드하면 내 공격을 경감).
- 각 슬롯 처리 후 HP가 0 이하면 종료. **동시 KO 타이브레이크(2026-07-15)**: 둘 다 0이면 **그 턴 시작 시점의 HP 비율(HP/maxHp)이 높았던 쪽이 승리**, 정확히 같을 때만 무승부(`winner=null`). 랜덤 없음 → 락스텝 안전. (도입 근거: AI 시뮬에서 동시 KO 무승부가 전체의 16%까지 치솟음 → 사실상 0%로.)

이 "슬롯 내 우선순위"가 명세의 의미다: *내 카드 3장은 고른 순서대로 실행*되고, 각 슬롯에서 *나와 상대 중 누가 먼저인지*만 이동→수비→공격으로 정해진다.

턴 종료: **독안개 판정**(아래) → 쿨타임을 1씩 감소시킨 뒤, **이번 턴 사용한 cd≥1 카드를 풀 쿨타임으로 잠금** → `turn += 1`. `planAffordable`은 **선택한 슬롯 순서대로**(회복·가드·공격의 기력 증감을 순서대로) 지불 가능 여부를 검사.

#### 전투 연출 — 공격 모션 · 필살기 컷인 (2026-07-23)
"앞뒤로 미끄러지기만 해서 루즈하다"는 피드백에 대응한 연출 레이어. **룰·수치는 건드리지 않는다**(순수 표현).
- **공격 모션**: 카드의 `fx` 종류별로 다른 동작(`ui.css`의 `atk-melee/atk-cast/atk-rush/atk-quake`). 근접은 웅크렸다 파고들고, 사격(`bolt/orb/flame`)은 기를 모았다 내지르고, `rush`는 잔상을 끌며 크게 돌진, `quake`는 솟구쳤다 내려찍는다. `FighterSprite`가 `is-atk-<fx>` 클래스를 붙여 선택. ⚠ 키프레임은 반드시 `scaleX(var(--flip))`을 유지해야 한다(빼면 공격 때마다 캐릭터가 뒤집힘).
- **타격감**: 모든 적중에 충격파 링(`.fx::before`), 근접엔 초승달 궤적(`.fx::after`). 피해 ≥ `SHAKE_DAMAGE`(22)면 판 전체가 흔들린다(`.board.is-shaking`).
- **필살기 컷인**: `signature: true` 카드가 발동하면(기력 부족 `nofuel` 제외) 타격 **직전에** 전투 화면 전체를 `CUTIN_MS`(1.75초) 동안 덮는다 — 대각 색띠가 쓸고 지나가며 대형 초상(`buildPortraitSvg`) + "〈캐릭터〉 · 필살기" + 기술명이 꽂힌다. 발동자가 나면 `cutin--me`, 상대면 `cutin--foe`(초상이 반대쪽으로). 구현은 `BattleScreen`의 `cutIn` 상태 + `ui.css`의 `.cutin*`.
- **멀티 안전**: 컷인·모션은 `resolveTurn` 결과(steps)를 보고 재생만 하므로 양 피어가 동일하게 재생 — 락스텝 결정론에 영향 없음. 다만 한 턴 해소 시간이 필살기당 1.75초 늘어난다(온라인 턴 타이머는 *선택* 단계에만 돌아 무관).

#### 전투 카드 표기 — 압축(compact) (2026-07-23)
`CardFace`에 `compact` prop 추가. **전투 화면(손패·슬롯)에서만** 켜서 설명문(`cardface__desc`)을 빼고 **이름 → 수치(⚔데미지 ↦사거리 ⚡기력, 가드는 방어·기력) → 범위표** 순서로만 보여준다(`.cardface--compact`가 flex `order`로 수치를 범위표 위로). 전투 중엔 설명을 읽을 겨를이 없고 빠르게 고르는 게 우선이라는 판단. **덱 빌더·도감은 `compact` 없이 설명 그대로** — 카드를 익히는 화면이라 설명이 핵심.
- **아이콘은 조연**: 종류 아이콘(⚔🛡⚡✚)은 압축 카드에서 우상단 구석의 작은 표식으로 `absolute` 배치(12px·불투명도 0.5). 대신 이름 18px·수치 15px·범위표는 카드 폭을 꽉 채워 "보자마자 범위·공격력·기력"이 읽히게 한다.
- **예외 — 이동 카드**: 화살표가 곧 정보라 구석 아이콘 대신 본문 가운데에 큰 화살표(`cardface__moveart`, 40px)를 넣는다. 수치·범위표가 없어 비어 보이던 문제도 해결.
- 슬롯(116px)은 손패(138px)보다 좁아 `.slot .cardface--compact`에서 이름 13px·수치 12px로 한 단계 줄여 수치가 한 줄에 들어가게 한다.
- 설명이 빠진 만큼 손패 카드 높이를 202→146px, `.cards` 높이를 378→322px로 줄여 **남는 세로를 보드에 돌려줬다**(격자가 커져 위치 싸움이 잘 보인다).

#### 카드 선택 미리보기 — 상시 유지 (2026-07-23)
카드를 고르는 동안 **지금까지 고른 플랜의 결과**를 보드에 계속 띄운다(`BattleScreen`의 `planPreview`). 슬롯을 1→3 순서로 훑으며 이동 카드는 위치를 옮기고, 공격 카드는 *그 시점 위치* 기준 타격 셀을 계산 → 유령(`.fighter--ghost`)은 최종 예상 위치에, 빨간 셀은 공격 범위로 표시.
- **위치는 누적, 공격 범위는 누적하지 않는다**(2026-07-23 수정). 이동은 순서대로 쌓여 최종 위치를 보여주지만, 빨간 칸은 **마지막에 고른 공격 한 장의 범위만** 남긴다 — 여러 공격의 범위가 겹쳐 뜨면 어디를 때리는지 읽을 수 없다는 피드백. 새 공격을 고르면 그 공격 기준으로 교체된다.
- **꺼지는 조건은 슬롯이 전부 빌 때 + 실행(제출) 후**. 카드를 놓아도, 마우스를 떼도, 가드·원기처럼 위치와 무관한 카드를 올려봐도 예시는 남는다(예전엔 호버에만 묶여 배치 즉시 사라졌다). 단 **`phase !== 'select'`면 그리지 않는다** — 실행을 눌러도 슬롯은 해소가 끝날 때까지 채워진 채라, 이 조건이 없으면 진행 중인 전투 위에 예시가 겹쳐 남는다.
- 카드에 마우스를 올리면 그 카드 기준 미리보기가 **일시적으로 덮어쓰고**, 떼면 플랜 미리보기로 복귀. 호버 시엔 그 카드가 놓일 슬롯(`hoverSlot`) *앞*의 이동만 반영해 "이 카드를 넣으면 어디서 터지는지"를 보여준다.

#### 독안개 — 열(column) 기반 점진 축소 (2026-07-13 도입 → 2026-07-16 열 축소로 개편)
무한전(도망·회피 반복)을 확실히 끝내는 페이싱 장치. **양 끝 열부터 안쪽으로** 조여들어 결국 판 전체를 덮는다(세로 이동은 끝까지 자유 — 예전 "테두리 전체" 방식이 과했던 점 개선).
- **진행**: `FOG_START_TURN=6`에 stage 0 = **col 0·5**. `FOG_STEP_TURNS=3`턴마다 한 단계씩 안으로: **9턴** stage 1 = col 0·1·4·5, **12턴** stage 2 = col 0~5(**전부**). `fogStageAt(turn)`이 단계를, `isFogCell(cell, turn)`이 덮임 여부를 계산.
- **피해**: **턴 종료 시**(3슬롯 해소 후) 독안개 열에 서 있으면 **10(`FOG_DAMAGE`) 피해**. 실드/피해감소 미적용(고정), HP 0 가능(둘 다 0이면 동시 KO 타이브레이크).
- **경고**: `fogEscalatesNext(turn)`이 true인 턴(5·8·11턴)에 HUD 배지 — 5턴 "다음 턴부터 독안개!", 이후 "다음 턴 독안개 확대!".
- **종결 보장**: 12턴이면 모든 열이 독안개 → 어디 서 있어도 매 턴 -10 → 게임이 반드시 끝난다(AI 시뮬 1200판 무한전 0건, 최대 14턴).
- 구현: 상수·판정은 `types.ts`(`FOG_START_TURN/FOG_STEP_TURNS/FOG_DAMAGE/fogStageAt/isFogCell/fogEscalatesNext`), 적용은 `engine.resolveTurn` 끝(랜덤 없음 → **멀티 락스텝 결정론 유지**). 연출 `Step{phase:'fog', recoil:10}`. UI: 덮인 열 보라색(`.cell--fog`), HUD 배지(`.bhud__fog`). AI는 지금/다음 턴 독안개면 중앙 열로 이탈.

#### 지속 상태이상 — 독·화상·빙결 (2026-08-01, 3직업 개편 1단계)
3직업(전사/궁수/마법사) 개편의 **토대**로 먼저 엔진에 넣은 시스템. 이 시점엔 아직 **어떤 카드·유물도 쓰지 않으므로 기존 밸런스는 완전히 불변**이다(`npm run sim`·`sim:run` 결과 동일 확인).

- **설계 원칙**: ① 독안개와 **같은 자리**(턴 종료 정산·보호막 무시 고정 피해·랜덤 없음)에서 처리해 멀티 락스텝 결정론을 그대로 물려받는다. ② 종류를 늘리지 않는다 — 독·화상은 **출처만 다른 같은 지속피해**(궁수/마법사), 빙결은 이동만 막는다.
- **종류**: `StatusKind = 'poison' | 'burn' | 'frozen'`. 지속피해는 카드가 **위력**(턴당 피해)만 정하고 지속 턴은 `STATUS_TURNS`에 고정(독 3턴=길고 약하게, 화상 2턴=짧고 강하게) — 밸런스 손잡이를 한곳에 모은다. 빙결은 카드의 `freeze`가 턴 수를 직접 준다.
- **부여 조건**: 기절과 같은 규칙 — **피해가 실제로 들어갔을 때만**. 가드에 완전히 막힌 타격으로는 아무것도 안 묻는다.
- **중첩**: 같은 종류는 **한 칸으로 합친다**(항목이 늘면 스냅샷·UI가 무한정 자란다). 위력은 합산(`STATUS_POWER_CAP=30` 상한), 지속은 더 긴 쪽으로 갱신.
- ⚠ **지속피해와 빙결은 시간 규칙이 다르다.** 독·화상은 **걸린 턴에 바로 갉고** 그 턴에 지속도 같이 깎는다("맞자마자 타들어간다"가 읽히고, 한 턴 미루면 2턴짜리 화상이 사실상 1턴이 된다). 빙결은 **걸린 턴엔 지속을 안 깎는다** — 이동은 슬롯 우선순위상 공격보다 먼저 해소돼서 빙결을 건 턴엔 남은 슬롯의 이동만 막을 뿐 온전한 한 턴을 못 막기 때문. 같이 깎으면 "1턴 빙결"이 슬롯 몇 개만 막고 사라져 카드 설명과 어긋난다. 판정 기준은 `StatusEffect.since`(걸린 턴 번호).
- **빙결 ≠ 기절**: 빙결은 **이동만** 무효(카드는 소모되고 쿨다운도 돈다), 공격·가드는 그대로 나간다. 기절(`stunned`)은 그 턴 카드를 통째로 버린다.
- **유물 훅**(`Passive`): `poisonOnHit`·`burnOnHit`(평범한 카드에도 상태이상을 묻힌다) · `statusPowerPct`(내가 거는 지속피해 위력 +N%, **거는 순간 한 번 계산해 박는다** — 매 틱 다시 계산하면 도중에 얻은 유물이 이미 걸린 것까지 소급된다) · `bonusVsAfflicted`(상태이상 걸린 상대에게 추가 피해. **이 공격이 새로 거는 건 아직 안 걸린 것으로 본다** — 아니면 카드 한 장이 스스로 보너스를 켠다).
- **구현**: 타입·상수 `types.ts`(`StatusKind/StatusEffect/STATUS_TURNS/STATUS_POWER_CAP/isDot`), 적용·정산 `engine.resolveTurn`(`applyStatus`/`tickStatuses`, 독안개보다 **먼저** — 독안개는 무한전을 끊는 최후 장치라 마지막이 읽기 좋다). 연출 `Step{phase:'status', recoil:N}`(독안개와 같은 방식으로 본인 몸에 -N)·`result:'frozen'`. 검증 `npm run check`의 "상태이상" 절 18건.
- **미구현(다음 단계)**: 함정·지형은 **보드 셀 상태**가 필요한데 현재 보드엔 셀 상태 개념이 아예 없다(위치 2개 + 턴 번호가 전부). AI는 아직 상태이상을 모른다 — 빙결 중에도 이동 카드를 고른다(크래시는 없고 카드만 버려진다).

#### AI (`src/battle/ai.ts`)
2D·쿨타임 인식 그리디 3장 플랜: ⓪**위기 가드**(HP≤45면 첫 슬롯에 확률적으로 가드 — `panicGuard`, 2026-07-15) → ①**독안개 이탈**(지금/다음 턴에 안개 열이면 공격보다 우선해 중앙 열로 이동) → ②사거리에 들면 공격(난이도별 `aggression`) → ②′**회복**(2026-07-30) → ③기력 낮으면 원기 → ④상대와 행·열 정렬(접근/대시) → ⑤가끔 가드 → ⑥폴백. 쿨타임/잠금을 존중하며 슬롯마다 자기 위치·기력을 시뮬레이션.
- **②′ 회복(2026-07-30)**: 덱에 힐 카드가 있고 체력이 `char.maxHp`의 55% 이하이며 (힐 비용 + 최저가 공격)만큼 기력이 남을 때 힐을 낸다. 그전까지 AI는 `kind:'heal'`을 **한 번도 고르지 않아** `c-repair`가 든 덱(로그라이크 보상 풀·주술사)을 제대로 굴리지 못했다. 1:1 페이싱 영향은 미미(hard 6000판 평균 5.62→5.84턴, 중앙값 5 유지).
- **한계(알려진 것) 1 — 회피가 없다**: 상대의 사거리를 보고 **줄을 바꿔 피하는 로직이 없다**. 같은 줄을 훑는 원거리 카드(노바 블래스트·이온 랜스)에 특히 약해서, AI를 플레이어 대역으로 쓰는 시뮬(`npm run sim:run`)의 절대 수치는 사람보다 비관적으로 나온다 — 추세 비교용으로 읽어야 한다.
- **한계(알려진 것) 2 — 기력 문턱 인공물**: ③ 원기 회복 판단이 ④ 접근보다 먼저 와서, 기력이 `energyFloor`(hard 30) 바로 아래면 접근 대신 회복에 슬롯을 낭비한다. 그래서 "턴당 기력"이 그 문턱에 걸치는 캐릭터의 강도가 계단처럼 튄다(로그라이크 시뮬에서 VOLT 턴당 기력 8 vs 10이 클리어율 30% vs 50%). ③④를 맞바꿔 보니 1:1 밸런스가 무너져(VOLT 46%→67%, 평균 5.8→6.1턴) 되돌렸다 — 지금 순서가 캐릭터 간 기력 격차를 눌러주고 있다. **밸런스를 `turnEnergy`로 조정하지 말 것**(연속적인 훅으로 조정). 근본 해결은 AI의 자원 판단 개선.

---

## ⑤ 설계 명세 ↔ 구현 정합 상태  ✅결정됨: **방향 A (2D 격자로 재작업)**

> 2026-06-15, 사용자 요청("위 다른 점들 다 바꿔주세요")으로 **코드를 설계 명세(2D 격자)에 맞추는 방향 A**를 채택·구현 완료. 원작과 명세가 모두 2D 격자 + 범위 마스크를 가리키므로 이 방향이 의도에 부합. 브라우저에서 한 턴 실행까지 검증됨.

| 항목 | 명세(의도) | 현재 코드 | 상태 |
|---|---|---|---|
| **전장 형태** | 2D 격자(상·하·좌·우) | **2D 격자 6×3** (`{col,row}`) | ✅일치 |
| **이동 카드** | `> < ^ v` + `>> <<`(2칸), 쿨 0~1턴 | 4방향 1칸(쿨0) + 좌우 대시 2칸(쿨1) | ✅일치 |
| **가드** | 10에너지 / 50방어 / 쿨1 | 10에너지 / 50흡수(턴 단위 실드 풀) / 쿨1 | ✅일치 |
| **에너지 회복** | 50회복 / 쿨0 | 50회복 / 쿨0 | ✅일치 |
| **공격 범위 표현** | 십자/좌우 등 2D 형태 | `range` 오프셋 `{df,du}` 마스크 | ✅일치 |
| **실행 순서** | 슬롯 선택 순서대로(1→2→3), 슬롯 내에선 이동→수비→공격 우선순위 | 슬롯 순서 해소 + 슬롯 내 우선순위(move<defense<attack) | ✅일치 |
| **쿨타임 시스템** | 있음 | 카드별 `cooldown`, 선택·턴 단위로 관리 | ✅일치 |
| **한 턴 3장** | 3장 | 3장 | ✅일치 |
| **캐릭터** | 6직업 + 고유 카드 | 6명(각 고유 4장 + 특수 능력, 공용 약공/약가드 포함) | ✅일치 (2026-07-03) |
| **승리 후 보상** | (원작) 미스터리 카드 5중 1 선택 | 없음(건틀릿 진행만) | ❗미구현 — 다음 후보 |

### 명세 대비 추가/보강 결정 (구현하며 정한 것)
- **격자 크기 6열 × 3행** 확정(원작 스크린샷 기준). 변경 시 `types.ts`의 `GRID_COLS/ROWS`와 `ui.css`의 `.gridboard` grid-template를 함께 수정.
- **패시브 기력 회복 +20/턴** 추가(명세 외) — 원기 카드 쿨 중 교착 방지. 빼려면 `ENERGY_REGEN=0`.
- **공격은 동시 트레이드** — 같은 슬롯의 양측 공격을 동시 적용. 동시 KO는 턴 시작 HP 비율 타이브레이크(④ 참고, 2026-07-15).
- 공격 데미지/기력은 명세 앵커(십자 30en·40dmg, 좌우 40en·50dmg)에 맞춰 캐릭터별로 재조정(근접 20~30/10~15en, 중거리 30~40/20~40en, 시그니처 40~60/50~55en).

### 밸런스 패스 (2026-07-15) — "5턴 페이싱"
평균 3.5턴 → **5턴대**로 늦추는 대규모 수치 조정. `npm run sim [판수]`(`scripts/simulate.ts`, AI hard vs hard, 36 매치업)으로 데이터를 쌓아가며 반복 튜닝했다.
- **같은 공격 반복 금지(핵심 룰)**: 한 턴에 **같은 공격 카드를 두 번 넣을 수 없다**(쿨0이어도). 3공격 자체는 전략으로 유효하되 서로 다른 카드 3장이어야 하므로 기력이 넉넉할 때만 성립. UI(`placedNoRepeat`)·AI(`take`의 locked)가 강제, 엔진은 관대(락스텝은 양측 동일 입력이라 안전). 이동·브레이스 등 비공격 쿨0 카드는 반복 허용.
- **기력 경제**: 턴 재생 30→**20**, 원기 회복 50→**35**·쿨0→**1**, 시그니처 비용 상향(45~50) — 강한 3연타는 기력을 모아야 가능.
- **전체 HP 상향**: +10~15 (위 HP 표) — 페이싱·동시 KO 완화.
- **캐릭터 조정**: TITAN 피해감소 20→10·크러셔 30→25기력, AEGIS 방벽 30→15·잽 방벽 10→5·커튼 80→70, CIPHER 흡혈 10→15·HP+10·십자 20→15기력, EMBER 반동 10/20→5/10, VOLT 볼트 20→30기력·서지 흡수 20→10.
- **동시 KO 타이브레이크** 도입(④) — 무승부 16%→0%.
- **AI 개선**: 위기 가드(⓪)·안개 이탈 우선(①)·같은 공격 반복 금지 준수 — 사람에 가까운 플레이로 시뮬 신뢰도·CPU 품질 동시 확보.
- **최종 측정(500판×30 매치업=15,000판)**: 평균 **5.94턴**(중앙값 6), 무승부 **0.0%**, 승률 스프레드 **44.5%(CIPHER)~55.5%(EMBER)**. 남은 하드카운터: AEGIS>VOLT 84%, TITAN>CIPHER 81%, EMBER>TITAN 77% — 가위바위보 상성으로 의도 수용.

### 밸런스 패스 (2026-07-25) — 밀착 타격 도입 후 재조정
밀착 타격(④ 참고)으로 **빗나가던 공격이 명중하게 되자 밸런스가 크게 흔들렸다**. 도입 직후 시뮬(6,000판): 평균 **4.51턴**(6.12턴에서 급락), 승률 **22.8%(AEGIS)~78.1%(TITAN)** — 스프레드가 27.5%p→55.3%p로 2배. 근접 고화력(TITAN·CIPHER)이 유리해지고 거리를 벌려 싸우던 원거리형(VOLT·NOVA)이 무력화된 것이 원인. 시뮬을 6라운드 반복해 재조정했다.
- **전체 HP 상향(+20~30%)**: VOLT 115→157, TITAN 150→172, NOVA 125→162, CIPHER 120→145, AEGIS 140→164, EMBER 130→156. 명중률이 오른 만큼 체력을 올려 5턴대 페이싱을 회복.
- **TITAN 하향**: 해머 핸드 30→**29**dmg, 장갑판 피해감소 10→**9**. (밀착에서 싼 강타를 계속 꽂는 것이 지나치게 강했다.)
- **CIPHER 하향**: 크로스 슬래시 15→**24**기력, 패시브 흡혈 15→**10**. 난전에서 계속 회복하며 버티는 문제.
- **VOLT 상향**: 스파크 잽 20→**24**dmg. (아크 볼트는 30dmg·**29**기력으로 미세 조정.)
- **NOVA 상향**: 팜 펄스 20→**22**dmg, 노바 블래스트 35→**32**기력.
- **AEGIS 조정**: 실드 배시 30기력→**28**, 벌워크 드라이브 55→**50**기력.
- **EMBER 하향**: 오버히트 블리츠 40→**38**dmg·20→**26**기력.
- ⚠️ **비용 임계값은 데미지보다 훨씬 민감하다**: 체인 서지를 50→48기력으로 2 낮췄더니 VOLT 승률이 46%→**79%**로 폭등(기력 재생 30/턴과 맞물려 플랜 조합이 통째로 바뀜). 미세 조정은 **비용보다 데미지로** 하는 편이 안전하다.
- ⚠️ **수치 규약(10/5의 배수)의 예외**: 이번 패스의 값 일부는 규약을 벗어난다(HP 157/172/162/145/164/156, 해머 29dmg, 장갑판 -9, 볼트 29기력, 크로스 24기력, 배시 28기력, 블리츠 38dmg/26기력). **의도적이다** — 규약에 맞춰 10/5의 배수로 반올림해 봤더니 NOVA 승률이 50%→**15%**로, TITAN이 53%→**70%**로 붕괴했다. 특히 **장갑판 -9 → -10**이 치명적인데, 20데미지 카드가 절반(10)으로 잘려 저화력 캐릭터가 전멸한다. 가독성보다 밸런스를 택했으니 **반올림하지 말 것**.
- **최종 측정(500판×30 매치업=15,000판)**: 평균 **5.68턴**(중앙값 5), 무승부·타임아웃 **0.0%**, 승률 스프레드 **47.4%(VOLT)~53.0%(TITAN) = 5.6%p** — 2026-07-15 패스(11%p)보다 개선. 최대 하드카운터도 TITAN>NOVA **71.7%**로 완화(직전 84%). 상성 구조: TITAN이 원거리형(VOLT·NOVA)을 찍어누르고, EMBER·CIPHER가 TITAN을 잡고, NOVA가 AEGIS를 잡는 가위바위보.

### 남은 미구현 (다음 작업 후보)
- **뽑기/구매로 카드 획득 → 사용자가 덱을 조합해 출전**(2026-07-03 사용자 방침). 카드 소유 모델·재화·덱 규칙 설계 필요. `CharacterDef.cards` 분리는 이 확장을 염두에 둔 것.
- 승리 후 **미스터리 카드 5중 1 선택**(원작 보상). 덱 성장 시스템과 연결 필요.
- 신규 능력 조합 카드 추가·밸런싱(현재 6종 능력: drain/leech/pierce/push/selfShield/recoil).

---

## ⑤-bis 온라인 멀티플레이 (P2P + 짧은 코드) — 2026-06-18 추가 / 갱신

> 친구와 1:1 온라인 대전. **게임 데이터는 항상 P2P(WebRTC 데이터 채널)** 로 직접 흐른다. 연결 성사(시그널링)에만 약간의 중개가 필요하며, 두 가지 방식이 전송 추상화(`NetTransport`) 위에 올라간다:
> - **(기본) 빠른 대전 — 랜덤 매칭(2026-07-15 추가)** — `src/net/matchmaking.ts`. RTDB 대기열 `gridbrawl-mm`에서: ①살아있는(심장박동 45초 이내) 대기표를 오래된 순으로 **ETag CAS**(`lock` 필드, REST `X-Firebase-ETag`/`if-match`)로 원자 선점 → 게스트로 answer 작성. ②없으면 내 대기표(offer + `createdAt`/`aliveAt` 서버시각)를 걸고 answer 폴링(호스트), 20초마다 심장박동. 대기 중에도 4초마다 재스캔해 **나보다 엄격히 먼저 온**(createdAt, 동률이면 id) 대기표가 보이면 그쪽 게스트로 전환 — 둘이 동시에 큐를 눌러 서로 기다리는 교착을 풀고, 엄격한 나이순이라 서로를 동시에 잡는 역교착은 불가능. 연결 시도 12초 타임아웃 후 다음 후보, 5분 지난 대기표는 스캔 중 청소. RTDB 규칙은 `gridbrawl-mm`에 offer/answer/createdAt/aliveAt/lock만 허용.
> - **짧은 6자리 룸 코드** — Firebase Realtime Database를 *시그널링*으로만 사용(SDP 교환). 사용자가 "AD3EF1"처럼 짧은 코드를 원해 도입(2차 결정). 6자리 코드는 정보량상 *반드시* 중개소의 열쇠여야 하므로(연결정보=DTLS 지문+ICE+IP 등) 시그널링 서버 없이는 불가능.
> - **(폴백) 복붙 초대 코드** — 백엔드 0, 전체 SDP를 base64 코드로 직접 주고받음. `net/webrtc.ts`의 `createHost`/`joinAsGuest`로 구현돼 있으나 현재 로비 UI는 짧은 코드만 노출(오프라인/무설정 폴백용으로 코드 보존).
>
> 핵심 설계 의도: **전송 계층을 게임에서 분리**(`src/net/protocol.ts`의 `NetTransport`) → 시그널링 방식을 바꿔도(서버/WebSocket/매치메이킹) 전투·UI 불변.

### 화면 흐름
`title → mp-select(캐릭터 선택, CharacterSelect 재사용) → mp-lobby(6자 코드) → mp-fight(전투) → mp-result(승/패)`. 타이틀의 **"온라인 대전"** 버튼으로 진입. (`src/App.tsx`)

### 설정 (온라인 사용 전 필수)
- `.env`에 `VITE_FIREBASE_DB_URL=<RTDB URL>` 지정(미설정 시 로비가 "설정 필요" 안내를 띄우고 온라인 비활성). Firebase RTDB 규칙에서 `gridbrawl` 경로 읽기/쓰기 허용. 자세한 절차는 `.env.example`.
- 시그널링 데이터는 작고 1회성이며 **연결되면 자동 삭제**. SDK 없이 **REST + 폴링**으로 구현(`net/firebase.ts`).

### 연결 핸드셰이크 (짧은 코드, Firebase 시그널링)
1. **호스트**: `방 만들기` → `createOffer()`(ICE 수집) → 6자 코드 생성, RTDB `gridbrawl/<코드>/offer`에 SDP 기록 → `answer` 폴링.
2. **게스트**: `참가하기` → 코드 입력 → `offer` 읽기 → `createAnswer()` → `gridbrawl/<코드>/answer`에 기록.
3. **호스트**: `answer` 수신 → `acceptAnswer()` → 데이터 채널 오픈, 방 삭제. 양쪽 `NetTransport` 획득.
- STUN(`stun.l.google.com:19302`) 사용 → 대부분 NAT 통과. **대칭 NAT는 TURN 릴레이 필요**(아직 없음, 추후 업그레이드). 같은 LAN/PC는 STUN 없이도 동작.
- 연결 직후 양측이 `hello{charId}`를 교환해 서로의 아바타를 알아낸다.

### 동기화 모델 — **결정론적 락스텝** (서버 없이 일치 보장)
- **핵심 전제**: `engine.resolveTurn(planA, planB)`은 **랜덤이 전혀 없는 결정론** 함수다(PvP엔 AI 미관여). 따라서 두 피어가 **동일 엔진**을 돌리고 매 턴 **카드 3장 ID만 교환**하면, 둘 다 같은 `resolveTurn`을 호출해 동일한 결과·애니메이션·상태에 도달한다.
- **정규 좌표(canonical) — 엔진 한정**: 엔진 상태는 양쪽 피어 모두 **호스트 = side 0(col 0 끝), 게스트 = side 1(col 5 끝)** 로 동일하게 구성. 이동 방향이 절대좌표(col +/-)이므로 엔진은 절대 미러링하지 않는다.
- 매 턴: 로컬 플레이어가 3장 확정 → `plan{turn, cards}` 전송 → 상대 plan 수신 → **양쪽 모두** `resolveTurn(side0Plan, side1Plan)` → 동일 진행. plan은 `turn` 번호로 태깅(순서 보장 채널이지만 버퍼링으로 위상차 흡수).
- **렌더링은 로컬 시점(2026-06-27)**: 각 피어는 **자기 캐릭터를 항상 왼쪽(오른쪽 바라봄)·상대를 오른쪽**에 본다. 엔진은 정규 좌표 그대로 두고 `BattleScreen`이 `localSide === 1`일 때만 화면 col을 좌우 반전(`flip`, `dcol = 5 - col`)해 렌더. 절대좌표 이동 카드가 깨지는 문제는, 반전 시 좌↔우 이동 카드의 **표시 라벨·화살표만 교체**(`faceCard`, id·엔진 입력은 정규 그대로)해서 "화살표 방향 = 화면에서 실제 가는 방향"을 보장하여 해결. 공격 카드 사정거리/예측 범위는 로컬이 항상 오른쪽을 보므로 "앞 = 오른쪽" 고정(미러 불필요). HUD도 자기 쪽을 왼쪽에 두고 **"나"** 배지. 각 피어는 자기 쪽(`localSide`)만 조작.
- 상대 연결 끊김: 대기 중 `getOpponentPlan`이 `null` 반환 → "상대 연결 끊김" 배너 후 메뉴 복귀.
- **턴 제한 30초(2026-07-17)**: 상대를 무한정 기다리는 루즈함을 없애는 장치. `BattleScreen`의 `turnSeconds` prop(온라인만 전달, 싱글·튜토리얼은 미지정)으로 카운트다운이 돌고(HUD `.bhud__timer`, 5초 이하 빨간 점멸), 0이 되면 **자동 제출**한다. 자동 제출 플랜은 항상 유효하게 만든다 — ①현재 3장이 완성·지불 가능하면 그대로 ②아니면 빈 슬롯을 **원기 회복**으로 메우고 ③그래도 기력이 모자라면 원기 회복 3장(비용 0·쿨0이라 언제나 유효). 각 피어가 **자기 플랜만** 자동 생성해 평소처럼 전송하므로 락스텝 결정론은 그대로(플랜을 주고받지, 재계산하지 않음). 수동/자동 이중 제출은 `submittingRef`로 차단.

### 검증 (2026-06-18, 브라우저 루프백)
- WebRTC 핸드셰이크 + 데이터 채널 + 메시지 왕복 OK(복붙·짧은 코드 양쪽).
- **2피어 락스텝 전투를 끝까지 실행 → 양쪽 엔진 최종 상태(hp·winner·turn) 완전 일치** 확인.
- Firebase 시그널링은 **RTDB REST를 모킹**해 호스트→6자 코드 생성→게스트 참가→폴링→연결→왕복까지 검증(실제 RTDB 없이 로직 확인). 실제 대전은 사용자의 `VITE_FIREBASE_DB_URL` 필요.

### 미구현(다음 후보)
- **재대결(rematch)**: 현재 한 연결당 1경기, 종료 시 연결 종료 후 메뉴. 양측 합의 핸드셰이크로 같은 연결 재사용 가능.
- TURN 릴레이(대칭 NAT), 방 만료/정리 강화, 입력 검증·안티치트(서버 전환 시), 무설정 폴백으로 복붙 코드 UI 노출.

---

## ⑥ 용어집
- **턴(Turn)**: 양측이 카드 3장을 골라 전역 페이즈로 해소하는 단위.
- **페이즈(Phase)**: 한 턴의 해소 단계 — `move`(이동) → `defense`(수비) → `attack`(공격).
- **셀(Cell)**: 격자 칸 `{ col, row }`. 파이터의 위치 단위.
- **오프셋(Offset)**: 공격 `range`의 한 칸 `{ df, du }` — `df`=앞쪽(+상대 방향), `du`=위쪽(+위). `facing`으로 좌우 반전되어 실제 셀로 변환.
- **facing**: 바라보는 방향. p0=+1(오른쪽), p1=−1(왼쪽).
- **실드(shield)**: 가드로 쌓는 턴 단위 피해 흡수 풀(가드 1장 = +50).
- **쿨타임(cooldown)**: 카드 사용 후 잠기는 턴 수. `state.cooldowns[p][id]`로 추적, 선택 시 0이어야 사용 가능.
- **스텝(Step)**: `resolveTurn`이 반환하는 개별 해소 결과(애니메이션 1컷). 행동자·결과·데미지·스냅샷 포함.
- **건틀릿(Gauntlet)**: 나머지 파이터를 차례로 모두 상대하는 토너먼트 구조.
- **시그니처(signature)**: 캐릭터의 대표 강공격(코드의 `signature: true`).
- **독안개(Fog)**: 6턴부터 양 끝 열(col 0·5)에서 시작해 3턴마다 안쪽으로 조여들어 12턴에 판 전체를 덮는 지속 피해 구역(턴당 10). 무한전 종결 보장.

---

## 파일 지도 (어디를 보면 되나)
| 영역 | 파일 |
|---|---|
| 전투 타입/상수 | `src/battle/types.ts` |
| 카드 정의(공용) | `src/battle/cards.ts` |
| 전투 엔진(해소 로직) | `src/battle/engine.ts` |
| CPU AI | `src/battle/ai.ts` |
| 캐릭터/공격 카드 | `src/data/roster.ts` |
| 토너먼트 구조 | `src/game/tournament.ts` |
| 화면 흐름 | `src/App.tsx`, `src/ui/screens/*` |
| 멀티 전송 추상화(프로토콜) | `src/net/protocol.ts` |
| 멀티 WebRTC 프리미티브·복붙 폴백 | `src/net/webrtc.ts` |
| 멀티 짧은 코드 시그널링(Firebase) | `src/net/firebase.ts` |
| 멀티 턴 플랜 교환(락스텝) | `src/net/session.ts` |
| 멀티 로비 UI | `src/ui/screens/MultiplayerLobby.tsx` |
| 온라인 설정 가이드 | `.env.example` |

---

## 참고 출처 (Inuyasha: Demon Tournament 룰 리서치)
- [Inuyasha: Demon Tournament — Walkthrough, Tips, Review (JayIsGames)](https://jayisgames.com/review/inuyasha-demon-tournament.php)
- [Inuyasha Demon Tournament — NamuWiki](https://en.namu.wiki/w/%EC%9D%B4%EB%88%84%EC%95%BC%EC%83%A4%20%EB%8D%B0%EB%AA%AC%20%ED%86%A0%EB%84%88%EB%A8%BC%ED%8A%B8)
- [Inuyasha Demon Tournament — Play-Games](https://www.play-games.com/game/15212/inuyasha-demon-tournament.html)
- [InuYasha: Demon Tournament — Flash Game (evilgames.eu)](https://evilgames.eu/flashgames/inuyasha-demon-tournament.htm)

*최종 갱신: 2026-07-13 (독안개 — 가장자리 축소 페널티 추가)*

---

## ⑥ 덱 빌딩 (2026-07-22 · 1단계 MVP)

전투 전에 자기 덱을 짜서 캐릭터·플레이어마다 다른 구성으로 싸운다. 구현은 `src/game/decks.ts`.

### 카드 분류
- **고정(7장)**: 이동 `m-up/m-down/m-right/m-left`(1칸), 지원 `c-brace`(기본 방어)·`c-energy`(원기) + **그 직업의 기본 공격 `basics[0]`**. ⚠ 일곱 번째가 캐릭터마다 다르므로 상수가 아니라 함수다(`fixedCardsFor(charId)`) — 2026-08-04 전에는 공용 `c-strike`로 고정이었다.
- **선택 풀(직업별, 이 중 7장)**: 이동 `m-right2`/`m-left2`(대시)·대각 4종, 공격 `c-shot` + **그 직업의 나머지 기본기 2장** + 그 직업 고유 공격, 지원 `c-guard`(더 좋은 방어)·`c-repair`(힐) + 고유 지원(전사 `war-wall`). ⚠ 직업마다 고유 카드 수가 다르다(전사 5·궁수 4·마법사 4).
- 전투 덱 = 고정 7 + 고른 7 = **14장**. 모든 덱 카드는 `deckFor(char)`(공용+기본기+고유)의 부분집합이므로 **멀티 락스텝의 상대 플랜 복원(`net/session.ts`)은 수정 없이 동작**한다.

### 저장·조립
- `Deck { id, name, charId, cardIds[7] }`. 로컬 저장은 `src/game/decks.ts`(localStorage), **계정 동기화는 `src/game/deckSync.ts`**.
- **계정별 저장(2026-07-23)** — 구글 로그인 사용자는 RTDB `decks/<uid>`가 원본(source of truth)이고 localStorage `gb-decks:<uid>`는 오프라인 캐시. 게스트/DB 미설정은 `gb-decks` 로컬 전용.
  - UI는 `deckSync`의 `listDecks()/putDeck()/removeDeck()`만 쓴다(비동기). 읽기 실패 시 캐시로 폴백해 오프라인에서도 플레이 가능, 쓰기는 **로컬 먼저 → 클라우드 업로드** 순.
  - RTDB는 `net/firebase.ts`와 같이 **REST 전용**, 권한은 Auth ID 토큰(`?auth=`, `net/auth.ts`의 `getIdToken()`). 규칙은 `database.rules.json`의 `decks/$uid`(본인만 읽기/쓰기 + 필드 검증) → 규칙 변경 시 `--only database` 배포 필수.
  - 로그인 직후 1회, 게스트 시절 로컬 덱을 계정으로 올린다(`gb-decks-migrated:<uid>` 플래그).
- `assembleDeck(deck)` = `fixedCardsFor(deck.charId)` + 고른 카드 → `BattleScreen`의 `deck` prop(로컬 손패).
- `PRESET_DECKS`(고유4 + 펄스샷 + 가드 + 오른쪽 대시) = 봇 상대 덱 + 저장된 덱이 없을 때의 기본 덱.

### 화면·흐름
- `title →(게임 시작)→ run-start …`(로그라이크 — 2026-08-04부터 타이틀의 **게임 시작**이 곧 로그라이크다) | `title →(PVP)→ deck-select → mode-select →` 봇전 `fight → result` | 온라인 `mp-lobby → mp-fight → mp-result`.
- `deck-select →(덱 만들기)→ deck-manage`(목록·수정·삭제) `→ deck-build`(캐릭터 선택 + 풀에서 7장 토글 + 이름 저장). ⚠ 2026-08-04부터 **타이틀에는 덱 만들기가 없다** — 덱은 PVP에서만 쓰므로 PVP 안(덱 선택 화면 헤더)으로 옮겼고, `deck-manage`의 뒤로도 `deck-select`로 간다.
- **봇전은 1:1 단판** — 상대는 나와 다른 캐릭터를 랜덤으로 뽑고 그 캐릭터의 프리셋 덱을 쓴다(`decideAI`의 `availableCards` 인자).

### 남은 과제 (2단계)
- 선택 풀 확장: **대각선 이동 4방향**(`MoveDir`·`MOVE_DELTA`·카드 아이콘·멀티 미러링), **HP 힐 지원 카드**(엔진 `resolvePrep`에 회복 처리). 현재 풀은 캐릭터당 ~8장이라 7장 고르기의 선택 폭이 얇다.
- 건틀릿(`game/tournament.ts`)은 단판 전환으로 미사용 — 정리 또는 별도 모드로 부활 검토.
